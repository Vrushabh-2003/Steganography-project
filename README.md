# Steganography-project
A C-based Steganography project for securely hiding secret information inside BMP images. It implements both encoding and decoding of secret data using bit manipulation and LSB techniques. The project covers image processing, file handling, pointers, bitwise operations, command-line arguments, binary data handling,validation of encoded information.
